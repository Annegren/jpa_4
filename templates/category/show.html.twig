{# templates/product/show.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}{% endblock %}

{% block body %}

{% if products %}
    <h1 class="mb-5">Les voyages disponible pour:
      {{ products.0.category.name }} et moi
    </h1>
    {% for product in products %}

<section class="container p-5 col-5">
<div class="card" style="width: 30rem;">
  <img src="{{product.poster}}" class="card-img-top" alt="...">
  <div class="card-body">
    <h5 class="card-title">{{  product.title }}</h5>
    <p class="card-text">{{ product.description }}</p>
        <a href="{{path("product_show", { id: product.id }) }}">En savoir plus</a>
  </div>
</div>
</section>
  
  
    {% else %}
    aucun voyage trouvé
    {% endfor %}

{% endif %}
    <a href="{{ path('category_index') }}">
      Retour 
    </a>

{% endblock %}
